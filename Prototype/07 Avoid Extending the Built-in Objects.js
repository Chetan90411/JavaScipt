
Array.prototype.shuffle = function () {
    //...
}

const array = [];
array.shuffle();

// don't modify Objects you don't own
// don't overwrite methods, don't add new properties and methods and don't remove existing properties of a built-in Object

// there might be code which will be dependent on the built in object so modifying them may cause all sorts of issues